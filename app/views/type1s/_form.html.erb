<%= form_with(model: type1, local: true) do |form| %>
  <% if type1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type1.errors.count, "error") %> prohibited this type1 from being saved:</h2>

      <ul>
      <% type1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
